# Public Vite client configuration for production builds.
# Convex deployment URLs are intentionally public and are embedded in the browser bundle.
VITE_CONVEX_URL=https://aware-sandpiper-557.convex.cloud
